spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fdc?characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: create

fdc:
  base-url:
  file-storage:
    type:
    local-temp-path: